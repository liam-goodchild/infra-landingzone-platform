# infra-landingzone-platform

Terraform root module for the Sky Haven Azure landing zone platform layer. It manages the management group hierarchy, hub networking, public DNS delegation, and subscription budgets that form the shared platform foundation.
